[package]
name = "s1-window"
version = "0.1.0"
edition = "2024"

[dependencies]
bytemuck = "1"
pollster = "1"
wgpu = "30"
winit = "0.30"

# S1 の計測は release で行う。バイナリサイズと起動時間の比較のため
# 実際の配布ビルドに近い設定にする。
[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
