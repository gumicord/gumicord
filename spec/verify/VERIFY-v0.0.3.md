# v0.0.3 実機確認手順書 (デスクトップ展開)

`NEXT.md` の v0.0.3 項目をなぞるための手順と記録用紙である。
合否の判断は目視・操作で行い、結果を残す。見つけた不具合は直さず記録する。
実行時健全性の確認はテスターに委ねる (ADR-0009 のとおり CI の成功を
動作確認済みとはしない)。

## 対象ビルド

- 入手元: GitHub Actions の Release (手動実行) または CI の dist 成果物
- 版 / SHA:
- 実施日:
- 実施者:
- OS / 版 (macOS 15 / Ubuntu 24.04 など):

## 1. 同一テーマ

`examples/themes/midnight` と `examples/themes/wallpaper` を
`GUMICORD_THEME` で差し替えて起動する。

- [ ] 背景画像が出て、UI の半透明合成が Windows 版と同じに見える
- [ ] テーマを編集すると再起動なしに反映される
- [ ] 読めない・壊れた編集では直前の成功版を維持する

結果:

## 2. 同一プラグイン

見本は `examples/plugins/dev.gumicord.hello`。

- [ ] 見本の変形が画面に反映されること
- [ ] 応答しないプラグインがあっても描画が止まらず、隔離されること
- [ ] セーフモード (`GUMICORD_SAFE_MODE=1`) では読み込まず起動する

結果:

## 3. 日本語入力 (D3)

- [ ] 日本語をインライン変換で入力でき、変換候補ウィンドウが出る
- [ ] 候補ウィンドウが入力欄に重ならず、欄の近くに出る
- [ ] 確定した文字が送信できる
- [ ] 使用した IME (macOS: ことえり／Google 日本語入力、Linux: fcitx5 / ibus):

結果:

## 4. クリップボード

- [ ] 本文の複写・貼り付けが他のアプリと往復できる
- [ ] 画像の複写・貼り付けができる (対応範囲は機種依存)

結果:

## 5. ファイル選択

v0.0.3 では基盤のみ。呼び出す画面はまだ無い (添付送信は M2)。
開発機での確認手順:

```sh
cargo run -p gumicord-platform --example pick_file
```

- [ ] OS ネイティブの選択画面が開く (Windows／macOS／Linux)
- [ ] 選んだ画像のパスが表示される。取り消しは `no choice` になる

結果 (開発者が実施し、OS ごとに記録):

## 6. 配布物と起動

- [ ] Linux: AppImage がそのまま起動する
- [ ] macOS: dmg を開いて起動する (未署名の警告が出る。署名は別作業)
- [ ] Windows: ダブルクリックでコンソール窓が出ない
- [ ] ログが残る (Windows: `%APPDATA%\gumicord\logs`、
  macOS／Linux: 設定先の `logs/`)。起動失敗時はここを見る
- [ ] Windows: VC++ 再頒布パッケージの無い機械でも起動する

結果:

## 7. 起動時クラッシュの回帰

v0.0.2 世代は、箇条書きを 2 つ以上含む発言が見えると起動直後に
`duplicate child` で落ちていた (accesskit への子 id 重複)。
復帰したセッションで確認する:

- [ ] 箇条書き・引用・コードブロックを含むチャンネルを開いても落ちない
- [ ] ナレーター／VoiceOver を付けたまま起動しても落ちない

結果:

## 8. スクリーンショット比較 (X2)

- [ ] CI の `screenshot` ジョブが緑である
- [ ] Windows は祝福画像と照合されている (素通りでない)
- [ ] Linux の実像 (CI 成果物 `screenshot-ubuntu-*`) を目視し、
  差が意図どおりなら祝福画像として登録する

結果:

## 対象外

- macOS の署名・公証: Apple Developer Program の参加と secrets 登録が要る。
  手順は `packaging/README.md`。未設定なら未署名のまま作られる
- Android のクリップボードと非 Windows のファイル選択の残りは v0.0.4
- 実行時健全性の網羅確認は引き続きテスター任せ

## 終了条件

- 上の結果欄がすべて埋まっていること
- 不具合は内容・再現手順つきで記録してあること
