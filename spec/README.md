# Gumicord 仕様書

**この `spec/` が単一の真実の源である。** 実装と仕様が食い違ったとき、直すのは実装のほうか、あるいはまず仕様を更新するかのいずれかであり、「仕様が古いまま実装だけ進む」状態を許さない。

## 目次

| 文書 | 内容 | 状態 |
|---|---|---|
| [00-vision.md](00-vision.md) | ビジョン・目標・非目標・リスク | 確定 |
| [01-requirements.md](01-requirements.md) | 要件定義 (ID 付き) | ドラフト |
| [02-architecture.md](02-architecture.md) | アーキテクチャ | **ドラフト** — 描画とバックエンドは確定、IME 層は要更新 |
| [03-uitree.md](03-uitree.md) | セマンティック UI ツリー仕様 (拡張 ABI) | **ドラフト** — M1 の安定 ID セットを提案 |
| [04-theme.md](04-theme.md) | テーマ仕様 | **ドラフト** — M1 の範囲を定義 + JSON Schema |
| [05-plugin-api.md](05-plugin-api.md) | プラグイン API 仕様 | **ドラフト** — 実行モデルの節のみ確定 (S3) |
| [06-renderer.md](06-renderer.md) | レンダラ仕様 | 未執筆 |
| [07-roadmap.md](07-roadmap.md) | マイルストーン | 未執筆 |
| [08-spike-plan.md](08-spike-plan.md) | 技術検証計画 | **完了** |
| [09-discord-protocol.md](09-discord-protocol.md) | Discord プロトコル仕様 | **ドラフト** — S4 で実測した範囲 |

スパイクフェーズは完了した ([08-spike-plan.md](08-spike-plan.md))。残る `04-theme.md` / `06-renderer.md` / `07-roadmap.md` を順次執筆する。

## アーキテクチャ決定記録 (ADR)

| ADR | 決定 | 状態 |
|---|---|---|
| [0001](adr/0001-native-rust-renderer.md) | UI レンダラを Rust + wgpu で自前実装する | 承認 |
| [0002](adr/0002-quickjs-plugin-runtime.md) | プラグインランタイムに QuickJS、開発言語に TypeScript を採用する | 承認 |
| [0003](adr/0003-monorepo.md) | 単一のモノレポで開発する | 承認 |
| [0004](adr/0004-semantic-uitree-as-extension-abi.md) | セマンティック UI ツリーを唯一の拡張 ABI とする | 承認 |
| [0005](adr/0005-ime-strategy.md) | IME テキスト入力層を自前実装する | 承認 |

## ルール

### 1. 仕様が先

実装の PR は、対応する要件 ID (`FR-024`, `EXT-032` など) を参照する。参照先がない実装は、まず仕様を書くところから始める。

### 2. 決定は ADR に残す

「なぜこうしたか」「何を却下したか」「どういう条件で見直すか」を書く。特に**却下理由**を書く。書かないと同じ議論を半年後に繰り返す。

ADR は原則として書き換えない。決定を覆すときは新しい ADR を起票し、古い ADR のステータスを `置換 (ADR-XXXX による)` に変更する。

### 3. 拡張 ABI は不可侵

[03-uitree.md](03-uitree.md) の安定 ID は、メジャーバージョン内で破壊的変更をしない (`EXT-003`)。追加のみを許す。

これは技術的な制約ではなく**プロジェクトの約束**である。BetterDiscord のプラグインが壊れ続ける問題を解くことが Gumicord の存在理由であり、この約束を破ると存在理由が消える。

### 4. 推測で決めない

性能・IME・プラットフォーム制約は、実測してから仕様に落とす。数値目標を書くときは根拠となる測定を ADR に紐づける。

暫定の数値には「要スパイク」と明記し、確定した数値と区別する。

### 5. 要件 ID は永久欠番

要件を削除しても ID を再利用しない。廃止した要件は `廃止` としてマークして残す。過去のコミットが参照している ID の意味が変わることを防ぐ。
