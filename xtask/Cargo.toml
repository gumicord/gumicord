[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

# xtask は依存を持たない。
# タスクランナーのために外部ツールの導入を要求しないことが目的なので、
# ここに依存を足すのは本末転倒である。
[dependencies]
