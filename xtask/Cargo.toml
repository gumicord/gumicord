[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

# 外部ツールの導入を要求しないことが xtask の目的である。
# ワークスペース内のクレートへの依存はこれに反しない。
# 安定 ID を Rust ソースからパースするより、定義元をそのまま使うほうが確実。
[dependencies]
gumicord-uitree.workspace = true
