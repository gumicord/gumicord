[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

# Requiring no external tool is xtask's whole purpose; depending on a
# crate inside the workspace does not break that, and using the
# definition directly beats parsing the stable IDs out of the source.
[dependencies]
gumicord-uitree.workspace = true
